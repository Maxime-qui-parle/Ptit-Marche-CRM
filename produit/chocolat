<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Menu pastel - Avec avoine, lait, noir</title>
  <style>
    :root{
      --btn-width: 160px;
      --btn-height: 320px;
      --radius: 0px;
      --gap: 0px;

      --vert:  #C8FAD3;
      --rouge: #FFB3B3;

      --txt: #2b2b2b;
    }

    *{box-sizing:border-box;margin:0;padding:0}
    html,body{height:100%;font-family:system-ui,-apple-system,Segoe UI,Roboto,"Helvetica Neue",Arial}
    body{
      display:flex;
      align-items:center;
      justify-content:center;
      background: linear-gradient(to bottom, #FFD9B3, #FFE7CC); /* fond orange pastel */
      color:var(--txt);
    }

    .menu{
      display:flex;
      gap:var(--gap);
      align-items:center;
      justify-content:center;
    }

    .btn{
      width:var(--btn-width);
      height:var(--btn-height);
      border:none;
      border-radius:var(--radius);
      cursor: pointer;
      font-weight:700;
      font-size:18px;
      display:flex;
      align-items:center;
      justify-content:center;
      text-align:center;
      transition: transform 200ms cubic-bezier(.2,.9,.2,1), box-shadow 200ms;
      position:relative;
      user-select:none;
      color: var(--txt);
      padding: 8px;
      overflow:hidden;
      background-repeat:no-repeat;
    }

    .btn:hover,
    .btn:focus{
      transform: scale(1.06);
      transform: translateY(-10px) scale(1.06); /* monte de 10px + zoom */
      box-shadow: 0 18px 44px rgba(30,26,50,0.14);
      z-index:10;
    }

    .title{ z-index:2; pointer-events:none; line-height:1.05; white-space:pre-wrap; }

    .info{
      position:absolute;
      z-index:4;
      left:50%;
      transform: translateX(-50%) translateY(8px);
      bottom:12px;
      min-width: calc(var(--btn-width) - 20px);
      background: rgba(255,255,255,0.90);
      box-shadow: 0 10px 26px rgba(30,26,50,0.10);
      border-radius: 10px;
      padding: 8px 10px;
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
      text-align:center;
      color:var(--txt);
      pointer-events:none;
      opacity:0;
      transition: opacity 200ms ease, transform 200ms ease;
      font-size:14px;
    }

    .info .label{ font-weight:600; margin-bottom:6px; }
    .info .price{ font-weight:700; font-size:14px; }
    .btn:hover .info,
    .btn:focus .info{
      opacity:1;
      transform: translateX(-50%) translateY(0);
      animation: pulseOpacity 1.6s ease-in-out infinite;
    }

    .vert { background-color: var(--vert);  }
    .rouge{ background-color: var(--rouge); }

    /* bouton image */
    .image-btn{
      padding:0;
      background:transparent;
      display:flex;
      align-items:center;
      justify-content:center;
    }

    .image-btn img{
      width:100%;
      height:100%;
      object-fit: contain;
      pointer-events:none;
      user-select:none;
      border:none;
    }

    @media (max-width:900px){
      .menu{ flex-direction:column; }
    }
  </style>
</head>
<body>
  <nav class="menu" aria-label="Menu principal">
    <!-- Bouton 1 : avoine -->
    <button class="btn image-btn" aria-label="Avoine crémeux">
      <img src="https://raw.githubusercontent.com/Maxime-qui-parle/Ptit-Marche-CRM/refs/heads/main/produit/photo%20produit/avoine%20cr%C3%A9meux.png" alt="Avoine crémeux">
      <div class="info">
        <div class="label">avoine crémeux</div>
        <div class="price">5,00$</div>
      </div>
    </button>

    <!-- Bouton 2 : lait -->
    <button class="btn image-btn" aria-label="Lait">
      <img src="https://github.com/Maxime-qui-parle/Ptit-Marche-CRM/blob/main/produit/photo%20produit/lait.png?raw=true" alt="Lait">
      <div class="info">
        <div class="label">lait</div>
        <div class="price">5,00$</div>
      </div>
    </button>

    <!-- Bouton 3 : noir -->
    <button class="btn image-btn" aria-label="Noir">
      <img src="https://github.com/Maxime-qui-parle/Ptit-Marche-CRM/blob/main/produit/photo%20produit/chocolat-noir.png?raw=true?raw=true" alt="Noir">
      <div class="info">
        <div class="label">noir</div>
        <div class="price">5,00$</div>
      </div>
    </button>

    <!-- Bouton 4 : caramel salé -->
    <button class="btn image-btn" aria-label="Noir">
      <img src="https://raw.githubusercontent.com/Maxime-qui-parle/Ptit-Marche-CRM/refs/heads/main/produit/photo%20produit/caramel%20sal%C3%A9.png?raw=true?raw=true" alt="Noir">
      <div class="info">
        <div class="label">caramel salé</div>
        <div class="price">5,00$</div>
      </div>
    </button>

    <!-- Bouton 5 : amande -->
    <button class="btn image-btn" aria-label="Noir">
      <img src="https://raw.githubusercontent.com/Maxime-qui-parle/Ptit-Marche-CRM/refs/heads/main/produit/photo%20produit/amande.png?raw=true?raw=true" alt="Noir">
      <div class="info">
        <div class="label">amande</div>
        <div class="price">5,00$</div>
      </div>
    </button>
  </nav>
</body>
</html>
